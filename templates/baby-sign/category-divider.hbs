<!-- templates/baby-sign/category-divider.hbs -->
<!DOCTYPE html>
<html>

<head>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;600;700&family=Lato:wght@400;700&family=Open+Sans:wght@400;600;700&display=swap" rel="stylesheet">
  <style>
    @page {
      size: A4;
      margin: 0;
    }

    body {
      margin: 0;
      padding: 0;
      font-family: {{customization.fontFamily}};
      background: {{categoryColor}};
      height: 297mm;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      color: white;
    }

    .category-name {
      font-size: 64px;
      font-weight: bold;
      text-align: center;
      margin: 20px 0;
    }

    .icon {
      font-size: 120px;
      margin-bottom: 30px;
    }
  </style>
</head>

<body>
  <div class="icon">
    {{#if (eq categoryName "Food & Drink")}}🍼{{/if}}
    {{#if (eq categoryName "Daily Routine")}}🛁{{/if}}
    {{#if (eq categoryName "Feelings & Emotions")}}😊{{/if}}
    {{#if (eq categoryName "Animals")}}🐶{{/if}}
    {{#if (eq categoryName "Family")}}👨‍👩‍👧{{/if}}
    {{#if (eq categoryName "Actions & Activities")}}🎵{{/if}}
  </div>
  <h1 class="category-name">{{categoryName}}</h1>
</body>

</html>
