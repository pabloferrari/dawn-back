<!-- templates/athlete-cookbook/favorites-meal-page.hbs -->
<!DOCTYPE html>
<html>

<head>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Lato:wght@400;600;700;800&display=swap" rel="stylesheet">
  <style>
    @page {
      size: A4;
      margin: 0;
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    html, body {
      height: 297mm;
      width: 210mm;
      font-family: {{customization.fontFamily}};
      color: #333;
      line-height: 1.3;
      overflow: hidden;
    }

    .page {
      height: 297mm;
      width: 210mm;
      padding: 8mm 10mm;
      display: flex;
      flex-direction: column;
    }

    /* Header */
    .page-header {
      height: 14mm;
      display: flex;
      justify-content: space-between;
      align-items: center;
      border-bottom: 3px solid {{meal.color}};
      padding-bottom: 2mm;
      margin-bottom: 3mm;
      flex-shrink: 0;
    }

    .header-left {
      display: flex;
      align-items: center;
      gap: 3mm;
    }

    .header-badge {
      display: flex;
      align-items: center;
      gap: 2mm;
      background: linear-gradient(135deg, #FFF9C4 0%, #FFECB3 100%);
      padding: 2mm 4mm;
      border-radius: 6px;
      border: 1px solid #FFD54F;
    }

    .header-badge-icon {
      font-size: 14px;
    }

    .header-badge-text {
      font-size: 10px;
      font-weight: 800;
      color: #F57F17;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    .header-meal {
      display: flex;
      align-items: center;
      gap: 2mm;
    }

    .header-meal-icon {
      font-size: 22px;
    }

    .header-meal-name {
      font-size: 18px;
      font-weight: 800;
      color: {{meal.color}};
    }

    .header-logo {
      height: 9mm;
      width: auto;
    }

    /* Recipes container */
    .recipes-container {
      flex: 1;
      display: flex;
      flex-direction: column;
      gap: 3mm;
      height: calc(297mm - 8mm - 8mm - 14mm - 3mm - 10mm);
    }

    /* Each recipe card */
    .recipe-card {
      flex: 1;
      height: calc((297mm - 8mm - 8mm - 14mm - 3mm - 10mm - 6mm) / 3);
      background: white;
      border-radius: 8px;
      border: 1px solid #E0E0E0;
      display: flex;
      flex-direction: column;
      overflow: hidden;
      position: relative;
    }

    .recipe-card::before {
      content: '';
      position: absolute;
      left: 0;
      top: 0;
      bottom: 0;
      width: 4px;
      border-radius: 8px 0 0 8px;
      background: {{meal.color}};
    }

    /* Recipe header */
    .recipe-header {
      height: 12mm;
      min-height: 12mm;
      padding: 1.5mm 3mm 1.5mm 4mm;
      display: flex;
      align-items: center;
      gap: 2.5mm;
      border-bottom: 1px solid #F0F0F0;
      flex-shrink: 0;
    }

    .recipe-icon {
      width: 11mm;
      height: 11mm;
      border-radius: 6px;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 18px;
      flex-shrink: 0;
      background: {{meal.color}}15;
    }

    .recipe-title-area {
      flex: 1;
      min-width: 0;
    }

    .recipe-title-row {
      display: flex;
      align-items: center;
      gap: 2mm;
    }

    .recipe-title {
      font-size: 14px;
      font-weight: 800;
      line-height: 1.2;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
      color: {{meal.color}};
    }

    .why-favorite {
      font-size: 9px;
      color: #666;
      font-style: italic;
    }

    .recipe-tags {
      display: flex;
      gap: 2px;
      margin-top: 1px;
    }

    .tag {
      padding: 2px 7px;
      border-radius: 8px;
      font-size: 8px;
      font-weight: 700;
      text-transform: uppercase;
      color: white;
    }

    .tag-protein { background: #7E57C2; }
    .tag-vegan { background: #43A047; }
    .tag-quick { background: #F06292; }
    .tag-gluten { background: #29B6F6; }

    .complexity-badge {
      margin-left: auto;
      font-size: 8px;
      font-weight: 700;
      padding: 2px 8px;
      border-radius: 10px;
      text-transform: uppercase;
    }

    .complexity-simple {
      background: #E8F5E9;
      color: #2E7D32;
    }

    .complexity-moderate {
      background: #FFF3E0;
      color: #E65100;
    }

    .complexity-complex {
      background: #FCE4EC;
      color: #C2185B;
    }

    /* Nutrition bar */
    .nutrition-bar {
      display: flex;
      align-items: center;
      gap: 3mm;
      padding: 1.5mm 3mm 1.5mm 4mm;
      background: linear-gradient(90deg, #F8F9FA 0%, #ECEFF1 100%);
      border-bottom: 1px solid #E0E0E0;
      flex-shrink: 0;
    }

    .macro-group {
      display: flex;
      align-items: center;
      gap: 3px;
    }

    .macro-icon {
      font-size: 10px;
    }

    .macro-value {
      font-size: 9px;
      font-weight: 700;
      color: #333;
    }

    .macro-unit {
      font-size: 8px;
      color: #666;
      font-weight: 400;
    }

    .macro-divider {
      color: #CCC;
      font-size: 10px;
    }

    .timing-info {
      margin-left: auto;
      display: flex;
      align-items: center;
      gap: 2px;
      font-size: 8px;
      color: #666;
      background: white;
      padding: 2px 6px;
      border-radius: 8px;
      border: 1px solid #E0E0E0;
    }

    .timing-icon {
      font-size: 9px;
    }

    /* Recipe content */
    .recipe-content {
      flex: 1;
      display: grid;
      grid-template-columns: 1fr 1.2fr;
      gap: 2mm;
      padding: 2mm 3mm;
      overflow: hidden;
    }

    /* Ingredients column */
    .ingredients-col {
      background: {{meal.color}}10;
      border-radius: 6px;
      padding: 2mm;
      overflow: hidden;
      display: flex;
      flex-direction: column;
    }

    .col-title {
      font-size: 10px;
      font-weight: 800;
      text-transform: uppercase;
      letter-spacing: 0.3px;
      margin-bottom: 1.5mm;
      display: flex;
      align-items: center;
      gap: 1.5mm;
      flex-shrink: 0;
      color: {{meal.color}};
    }

    .col-title-icon {
      font-size: 12px;
    }

    .ingredients-list {
      list-style: none;
      overflow: hidden;
      flex: 1;
    }

    .ingredients-list li {
      font-size: 10px;
      padding: 0.8mm 0;
      display: flex;
      align-items: flex-start;
      gap: 2mm;
      line-height: 1.3;
    }

    .ing-dot {
      width: 5px;
      height: 5px;
      border-radius: 50%;
      background: {{meal.color}};
      flex-shrink: 0;
      margin-top: 3px;
    }

    /* Steps column */
    .steps-col {
      overflow: hidden;
      display: flex;
      flex-direction: column;
    }

    .steps-list {
      list-style: none;
      overflow: hidden;
      flex: 1;
    }

    .steps-list li {
      font-size: 10px;
      padding: 1mm 0;
      display: flex;
      align-items: flex-start;
      gap: 2.5mm;
      line-height: 1.35;
    }

    .step-num {
      width: 16px;
      height: 16px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 9px;
      font-weight: 800;
      flex-shrink: 0;
      color: white;
      background: {{meal.color}};
    }

    .step-text {
      flex: 1;
    }

    /* Notes */
    .recipe-notes {
      margin-top: 1mm;
      padding: 1.5mm 2.5mm;
      background: #FFF9C4;
      border-radius: 4px;
      font-size: 9px;
      font-style: italic;
      color: #5D4037;
      display: flex;
      align-items: flex-start;
      gap: 1.5mm;
    }

    .note-icon {
      font-size: 10px;
      flex-shrink: 0;
    }

    .note-text {
      line-height: 1.3;
      overflow: hidden;
    }

    /* Coach note */
    .coach-note {
      margin-top: 1mm;
      padding: 1.5mm 2.5mm;
      background: linear-gradient(135deg, #E3F2FD 0%, #BBDEFB 100%);
      border-radius: 4px;
      font-size: 8px;
      color: #1565C0;
      display: flex;
      align-items: flex-start;
      gap: 1.5mm;
      border-left: 2px solid #1976D2;
    }

    .coach-icon {
      font-size: 10px;
      flex-shrink: 0;
    }

    .coach-text {
      line-height: 1.3;
      overflow: hidden;
    }

    .coach-text strong {
      color: #0D47A1;
    }

    /* Footer */
    .page-footer {
      height: 7mm;
      display: flex;
      align-items: center;
      justify-content: space-between;
      font-size: 9px;
      color: #888;
      border-top: 1px solid #E0E0E0;
      margin-top: 2mm;
      padding-top: 2mm;
      flex-shrink: 0;
    }

    .page-footer span {
      color: {{meal.color}};
      font-weight: 700;
    }

    .page-number {
      font-weight: 700;
      color: #666;
    }
  </style>
</head>

<body>
  <div class="page">
    <div class="page-header">
      <div class="header-left">
        <div class="header-badge">
          <span class="header-badge-icon">⭐</span>
          <span class="header-badge-text">{{section.title}}</span>
        </div>
        <div class="header-meal">
          <span class="header-meal-icon">{{meal.icon}}</span>
          <span class="header-meal-name">{{meal.name}}</span>
        </div>
      </div>
      <img src="{{images.banner}}" alt="Fuel Like a Runner" class="header-logo">
    </div>

    <div class="recipes-container">
      {{#each meal.recipes}}
      <div class="recipe-card">
        <div class="recipe-header">
          <div class="recipe-icon">
            <span>{{../meal.icon}}</span>
          </div>
          <div class="recipe-title-area">
            <div class="recipe-title-row">
              <div class="recipe-title">{{this.title}}</div>
            </div>
            {{#if this.whyFavorite}}
            <div class="why-favorite">"{{this.whyFavorite}}"</div>
            {{/if}}
          </div>
          {{#if this.prepComplexity}}
          <span class="complexity-badge complexity-{{this.prepComplexity}}">{{this.prepComplexity}}</span>
          {{/if}}
        </div>

        {{#if this.nutrition}}
        <div class="nutrition-bar">
          <div class="macro-group">
            <span class="macro-icon">🔥</span>
            <span class="macro-value">{{this.nutrition.calories}}</span>
            <span class="macro-unit">cal</span>
          </div>
          <span class="macro-divider">|</span>
          <div class="macro-group">
            <span class="macro-value">{{this.nutrition.carbs}}</span>
            <span class="macro-unit">carbs</span>
          </div>
          <span class="macro-divider">|</span>
          <div class="macro-group">
            <span class="macro-value">{{this.nutrition.protein}}</span>
            <span class="macro-unit">prot</span>
          </div>
          <span class="macro-divider">|</span>
          <div class="macro-group">
            <span class="macro-value">{{this.nutrition.fat}}</span>
            <span class="macro-unit">fat</span>
          </div>
          {{#if this.timing}}
          <div class="timing-info">
            <span class="timing-icon">⏱️</span>
            <span>{{this.timing.bestConsumed}}</span>
          </div>
          {{/if}}
        </div>
        {{/if}}

        <div class="recipe-content">
          <div class="ingredients-col">
            <div class="col-title">
              <span class="col-title-icon">🛒</span>
              Ingredients
            </div>
            <ul class="ingredients-list">
              {{#each this.ingredients}}
              <li>
                <span class="ing-dot"></span>
                <span>{{this}}</span>
              </li>
              {{/each}}
            </ul>
          </div>

          <div class="steps-col">
            <div class="col-title">
              <span class="col-title-icon">👨‍🍳</span>
              How to Make
            </div>
            <ol class="steps-list">
              {{#each this.preparation}}
              <li>
                <span class="step-num">{{add @index 1}}</span>
                <span class="step-text">{{this}}</span>
              </li>
              {{/each}}
            </ol>
            {{#if this.notes.length}}
            <div class="recipe-notes">
              <span class="note-icon">💡</span>
              <span class="note-text">{{#each this.notes}}{{this}}{{#unless @last}} • {{/unless}}{{/each}}</span>
            </div>
            {{/if}}
            {{#if this.coachNote}}
            <div class="coach-note">
              <span class="coach-icon">🏃</span>
              <span class="coach-text"><strong>From my experience:</strong> {{this.coachNote}}</span>
            </div>
            {{/if}}
          </div>
        </div>
      </div>
      {{/each}}
    </div>

    <div class="page-footer">
      <span>{{meal.name}}</span>&nbsp;•&nbsp;My Favorites ({{favoritesPageIndex}}/{{totalFavoritesPages}})
      <span class="page-number">Page {{pageNumber}}</span>
    </div>
  </div>
</body>

</html>
