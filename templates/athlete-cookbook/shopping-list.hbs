<!-- templates/athlete-cookbook/shopping-list.hbs -->
<!DOCTYPE html>
<html>

<head>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Lato:wght@400;600;700;800&display=swap" rel="stylesheet">
  <style>
    @page {
      size: A4;
      margin: 0;
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    html, body {
      height: 297mm;
      width: 210mm;
      font-family: {{customization.fontFamily}};
      color: #333;
      line-height: 1.3;
    }

    .page {
      height: 297mm;
      width: 210mm;
      padding: 8mm 10mm;
      display: flex;
      flex-direction: column;
    }

    /* Header */
    .header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      border-bottom: 2px solid #1B5E3F;
      padding-bottom: 2mm;
      margin-bottom: 3mm;
    }

    .header-logo {
      height: 9mm;
    }

    .header-title {
      display: flex;
      align-items: center;
      gap: 2mm;
    }

    .header-icon {
      font-size: 20px;
    }

    .header-text {
      font-size: 18px;
      font-weight: 800;
      color: #1B5E3F;
    }

    .header-week {
      font-size: 9px;
      color: #666;
      display: flex;
      align-items: center;
      gap: 2mm;
    }

    .week-line {
      width: 25mm;
      border-bottom: 1px solid #999;
    }

    /* Categories grid */
    .categories-container {
      flex: 1;
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 2.5mm;
      overflow: hidden;
    }

    /* Category card */
    .category-card {
      background: #FAFAFA;
      border-radius: 6px;
      padding: 2mm;
      display: flex;
      flex-direction: column;
      overflow: hidden;
    }

    .category-header {
      display: flex;
      align-items: center;
      gap: 1.5mm;
      padding-bottom: 1.5mm;
      border-bottom: 1px solid #E0E0E0;
      margin-bottom: 1.5mm;
    }

    .category-icon {
      font-size: 12px;
    }

    .category-name {
      font-size: 9px;
      font-weight: 800;
      color: #1B5E3F;
      flex: 1;
    }

    .category-count {
      font-size: 7px;
      color: #888;
      background: white;
      padding: 1px 4px;
      border-radius: 6px;
    }

    /* Items list */
    .items-list {
      list-style: none;
      flex: 1;
      overflow: hidden;
    }

    .item {
      display: flex;
      align-items: center;
      padding: 0.8mm 0;
      gap: 1.5mm;
    }

    .checkbox {
      width: 8px;
      height: 8px;
      border: 1.5px solid #1B5E3F;
      border-radius: 2px;
      flex-shrink: 0;
    }

    .item-name {
      flex: 1;
      font-size: 8px;
      color: #444;
      text-transform: capitalize;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
    }

    .item-count {
      font-size: 6px;
      color: #999;
      background: #EEE;
      padding: 1px 3px;
      border-radius: 6px;
      flex-shrink: 0;
    }

    .item-badge {
      font-size: 8px;
      flex-shrink: 0;
    }

    .other-essentials {
      background: #F5F5F5;
    }

    .other-essentials .category-header {
      border-color: #BDBDBD;
    }

    .other-essentials .category-name {
      color: #616161;
    }

    /* Write-in section */
    .write-in-section {
      margin-top: 2mm;
      padding: 2mm 3mm;
      background: #F5F5F5;
      border-radius: 6px;
    }

    .write-in-title {
      font-size: 9px;
      font-weight: 700;
      color: #1B5E3F;
      margin-bottom: 2mm;
    }

    .write-in-grid {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 2mm;
    }

    .write-in-item {
      display: flex;
      align-items: center;
      gap: 1.5mm;
    }

    .write-in-line {
      flex: 1;
      border-bottom: 1px dashed #CCC;
      height: 10px;
    }

    /* Tips section */
    .tips-section {
      margin-top: 2mm;
      padding: 2mm 3mm;
      background: linear-gradient(135deg, #E8F5E9 0%, #C8E6C9 100%);
      border-radius: 6px;
      display: flex;
      gap: 3mm;
    }

    .tip-item {
      flex: 1;
      display: flex;
      align-items: center;
      gap: 2mm;
      background: white;
      padding: 2mm;
      border-radius: 4px;
    }

    .tip-icon {
      font-size: 14px;
      flex-shrink: 0;
    }

    .tip-text {
      font-size: 7px;
      color: #555;
      line-height: 1.3;
    }

    /* Footer */
    .page-footer {
      margin-top: 2mm;
      text-align: center;
      font-size: 7px;
      color: #999;
    }

    .page-footer span {
      color: #1B5E3F;
      font-weight: 700;
    }
  </style>
</head>

<body>
  <div class="page">
    <div class="header">
      <img src="{{images.banner}}" alt="Fuel Like a Runner" class="header-logo">
      <div class="header-title">
        <span class="header-icon">🛒</span>
        <span class="header-text">Shopping List</span>
      </div>
      <div class="header-week">
        Week of: <span class="week-line"></span>
      </div>
    </div>

    <div class="categories-container">
      {{#each shoppingCategories}}
      <div class="category-card">
        <div class="category-header">
          <span class="category-icon">{{this.icon}}</span>
          <span class="category-name">{{this.name}}</span>
          <span class="category-count">{{this.items.length}}</span>
        </div>
        <ul class="items-list">
          {{#each this.items}}
          <li class="item">
            <span class="checkbox"></span>
            <span class="item-name">{{this.name}}</span>
            <span class="item-count">{{this.recipeCount}}</span>
          </li>
          {{/each}}
        </ul>
      </div>
      {{/each}}

      {{#if hasOtherEssentials}}
      <div class="category-card other-essentials">
        <div class="category-header">
          <span class="category-icon">🧺</span>
          <span class="category-name">Other Essentials</span>
          <span class="category-count">{{otherEssentials.length}}</span>
        </div>
        <ul class="items-list">
          {{#each otherEssentials}}
          <li class="item">
            <span class="checkbox"></span>
            <span class="item-badge">{{this.categoryIcon}}</span>
            <span class="item-name">{{this.name}}</span>
            <span class="item-count">{{this.recipeCount}}</span>
          </li>
          {{/each}}
        </ul>
      </div>
      {{/if}}
    </div>

    <div class="write-in-section">
      <div class="write-in-title">✏️ Additional Items</div>
      <div class="write-in-grid">
        {{#times 8}}
        <div class="write-in-item">
          <span class="checkbox"></span>
          <span class="write-in-line"></span>
        </div>
        {{/times}}
      </div>
    </div>

    <div class="tips-section">
      <div class="tip-item">
        <span class="tip-icon">🛒</span>
        <span class="tip-text">Shop the perimeter first—fresh foods are there</span>
      </div>
      <div class="tip-item">
        <span class="tip-icon">📅</span>
        <span class="tip-text">Buy enough for meal prep day (usually Sunday)</span>
      </div>
      <div class="tip-item">
        <span class="tip-icon">❄️</span>
        <span class="tip-text">Frozen fruits & veggies are just as nutritious</span>
      </div>
      <div class="tip-item">
        <span class="tip-icon">🔢</span>
        <span class="tip-text">Numbers show how many recipes use each item</span>
      </div>
    </div>

    <div class="page-footer">
      <span>Fuel Like a Runner</span> • Items sorted by frequency of use across all recipes
    </div>
  </div>
</body>

</html>
